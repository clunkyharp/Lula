# Store

Redux Toolkit store composition and slices.

Rules:
- State transitions must be predictable.
- Put domain state changes in slice reducers.
- Avoid storing transient UI-only details globally unless needed.
